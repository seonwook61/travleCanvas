apiVersion: v1
kind: Secret
metadata:
  name: trip-canvas-secrets
type: Opaque
stringData:
  # Replace with the production Supabase service role key. Do not use staging keys.
  SUPABASE_SERVICE_ROLE_KEY: "replace-me"
  # Replace with the production Google Maps server key.
  GOOGLE_MAPS_SERVER_API_KEY: "replace-me"
