apiVersion: v1
kind: Secret
metadata:
  name: trip-canvas-secrets
type: Opaque
stringData:
  # Replace with the staging Supabase service role key. Do not use production keys.
  SUPABASE_SERVICE_ROLE_KEY: "replace-me"
  # Replace with the staging Google Maps server key.
  GOOGLE_MAPS_SERVER_API_KEY: "replace-me"
